# 静态站：修改后需重新构建；Docker 使用同名 --build-arg，运行时 -e 不生效。
PUBLIC_ADS_ENABLED=false
PUBLIC_ADS_PROVIDER=sponsor

# sponsor 只使用 public/ 下的本地图；填写以 / 开头的站内图片路径。
# 图片与链接缺一时不显示槽位；链接仅支持完整的 http:// 或 https:// URL。
PUBLIC_SPONSOR_IMAGE=
PUBLIC_SPONSOR_URL=
PUBLIC_SPONSOR_NAME=赞助方
