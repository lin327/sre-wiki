apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sre-atlas-tls
  namespace: wiki
spec:
  secretName: sre-atlas-tls
  dnsNames:
    - wiki.tentative.me
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
