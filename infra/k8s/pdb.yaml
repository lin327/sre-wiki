apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: sre-atlas-pdb
  namespace: wiki
  labels:
    app: sre-atlas
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: sre-atlas
